export interface Course {
  id: string;
  title: string;
  provider: string;
  providerLogo: string;
  views: string;
  classes: string;
  image: string;
  estimatedRevenue?: string;
  category: string;
}
