// src/types/Course.ts
export interface Course {
  id: string;
  title: string;
  provider: string;
  providerLogo: string;
  views: string;
  classes: string;
  image: string;
  category: string;
  estimatedRevenue?: string;
}
