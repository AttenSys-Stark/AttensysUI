import React from "react";

const CourseNews = () => {
  return (
    <div className="bg-gradient-to-r from-blue-400 via-purple-300 to-purple-400 py-2 text-center text-white text-xs sm:text:base">
      🚀 Exciting news! AttenSys is now live in beta on Starknet testnet —
      explore, test, and shape the future of on-chain learning!
    </div>
  );
};

export default CourseNews;
